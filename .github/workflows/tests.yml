name: Tests
on:
  push:
    branches: [ main ]
env:
  CARGO_TERM_COLOR: always
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Tests
      run: cargo test --verbose